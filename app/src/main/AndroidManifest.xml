<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinDemoProject">
        <activity
            android:name=".SQLite.SQLiteActivity"
            android:exported="false" />
        <activity
            android:name=".RoomDB.RoomActivity"
            android:exported="false" />
        <activity
            android:name=".MVP.MVPActivity"
            android:exported="false" />
        <activity
            android:name=".MVC.Controller.MVCActivity"
            android:exported="false" />
        <activity
            android:name=".MVVM.Coroutine.view.MVVMActivity"
            android:exported="false" />
        <activity
            android:name=".MVVM.WithoutCoroutine.MVVMActivity"
            android:exported="false" />
        <activity
            android:name=".Dashboard.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KotlinDemoProject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>